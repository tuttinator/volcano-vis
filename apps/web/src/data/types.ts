export type AshComponent = {id:string;lower:number;upper:number;coordinates:number[][];leadHours:number;validAt:number;kind:'observed'|'estimated'|'forecast'};
export type Product = {id:string;productId:string;number:string;volcanoId:string;issuedAt:number;observedAt:number;estimated:boolean;components:AshComponent[];leadStates:Record<string,{validAt:number;status:'geometry'|'not-available'|'no-ash-expected'|'not-identifiable'}>;revision:number;supersededBy:string|null;sourceUrl:string;sourceLine:number;flags:{cloudObscured:boolean;lowForecastConfidence:boolean;correction:boolean}};
export type Volcano = {id:string;name:string;region:string;coordinates:[number,number];elevationMeters:number;timeZone:string;window:{start:number;end:number;preferredStart?:number;label:string;basis:'event'|'archive';notes:string}};
export type RasterFrame={id:string;volcanoId:string;date:string;timePrecision:'day';acquiredAt:null;layer:string;provider:string;url:string;sourceUrl:string;bounds:[number,number,number,number];crs:string;sha256:string;limitations:string};
export type CatalogEvent={id:string;volcanoId:string;label:string;start:number;end:number;basis:'event'|'archive';notes:string};
export type Catalog = {events:CatalogEvent[];imagery:RasterFrame[];version:number;retrievedAt:string;sourceUrl:string;coverage:string;inputSha256:string;volcanoes:Volcano[];products:Product[]};
export function editions(catalog:Catalog,volcanoId:string,eventId?:string){const event=catalog.events.find(e=>e.id===eventId);return catalog.products.filter(p=>p.volcanoId===volcanoId && !p.supersededBy&&(!event||(p.issuedAt>=event.start&&p.issuedAt<=event.end))).sort((a,b)=>a.issuedAt-b.issuedAt);}
export function displayDate(time:number,zone='UTC'){return new Intl.DateTimeFormat('en-GB',{timeZone:zone,day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit',hour12:false}).format(time);}

export function initialPositionLabel(product:Product){const status=product.leadStates['0'].status;return status==='not-identifiable'?'Ash not identifiable':status==='not-available'?'Position unavailable':status==='no-ash-expected'?'No ash expected':product.estimated?'Estimated ash position':'Observed ash position';}
