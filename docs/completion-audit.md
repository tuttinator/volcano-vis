# Completion audit — 12 September 2026

> Historical audit, not current publication status. See [article alignment](article-alignment.md)
> for the reviewed site implementation. Unresolved reuse questions below remain open.

This audit records the current prototype evidence and outstanding work. The full original plan is **not complete**; passing prototype tests does not close the scientific source gate.

## Current evidence

| Requirement | Current authoritative evidence | Assessment |
| --- | --- | --- |
| Real data and different blog presentations | `main.tsx` has explorer, editorial story, comparison, replay and directory; production browser tests exercise each | Implemented |
| Other volcanoes across Indonesia | Six volcano advisory collections in `catalog.json`; all 127 rows of the dated MAGMA table in `directory.json`; source-integrity and directory interaction tests | Implemented for available sequences and national directory |
| Official Type A/B/C labels | Retained MAGMA HTML, deterministic directory builder, input checksum, 76/30/21 row-count test; dated classification notes visible | Implemented as a 2021 source snapshot, not current alert levels. A source comparison now confirms that 77/29/21 was reported by BNPB in 2020 as well as Badan Geologi in 2026; BIG’s atlas conflicts too. The UI no longer implies a recent reclassification. Current named classifications remain unresolved; see `classification-review.md`. Directory type/region/search/selection persist in share links, with invalid-filter recovery tested. |
| Independent intuitive event windows | Krakatau defaults to 5 September WIB; other source sequences own their bounds; historical candidate dates in `event-windows.md` | Semeru’s November 2025 advisory episode is implemented separately from September 2026. Semeru now has six daily VIIRS crops for 19–24 November 2025. A ten-minute historical replay and other episode choices remain pending. |
| Regional map and time controls | MapLibre map; canonical replay time; playback/step/speed; keyboard; union of ticks and source instants | Implemented; long-session performance not benchmarked |
| Himawari Ash RGB | 579 locally preserved Band 13 infrared images plus 579 locally calibrated four-band Ash RGB scans; raw HSD and output checksums, processing manifest and browser test | Full event build exited successfully: 589 clock slots = 579 frames + 10 source gaps, zero failures. Coverage partition and all recorded input/output checksum tests pass. |
| VAA geometry by altitude and time | `replayAdvisories.ts` and `EventReplay.tsx`; real-data boundary, correction, estimate and multi-altitude tests; production overlay test | Implemented for recovered archive; morning editions absent. Initial assessment headings, badges, source-table cells and comparison cards distinguish unidentifiable ash from observed or estimated geometry. |
| No silently stale imagery/advisories | ±7-minute imagery selection; empty gaps; 60-minute initial-position display windows; forecasts only at exact targets | Selection logic and newer explicit empty forecast states tested. Production browser tests cover delayed superseded raster responses, HTTP failure clearing and recovery; sustained scrubbing performance remains unbenchmarked. |
| Eruption chronology | Six sourced markers, retained official HTML, chronology JSON, panel follows selected time | Implemented partial chronology; full report reconciliation remains open |
| Airport intervals | Nine identities across differing official reports, scheduled closure window and reopening instants, explicit unknown states | Partial evidence; actual closure-start intervals and original NOTAMs absent |
| One/two VIIRS snapshots | Twenty-six source-preserved daily crops, including Krakatau 5/6/7 September; checksum tests | Implemented as daily context; no precise overpass claims |
| Provenance and reuse constraints | URLs, source timestamps, checksums, source panels, inventory | Provenance implemented. `data-reuse-review.md` records NASA/JMA guidance and prepared credits; BOM advisory republication and Indonesian-provider dataset terms remain unresolved. GDACS hosting is not treated as a third-party licence grant. |
| Rebuild normalized datasets | `npm run data:build` invokes catalog, chronology and directory builders; resumable image fetchers preserve manifests | Implemented from retained inputs; not every original download automated |
| Impact/places and measurement explanations | Airport evidence; IR explanation; observed/estimated/forecast distinctions | Source-linked measurement guide now compares infrared, Ash RGB, SO₂, thermal anomalies and PM2.5 in the story and replay, explicitly distinguishing available layers from explanations. Eight airport reference points are now sourced from retained official DGCA profiles and shown on a separate replay map; selection follows the clock’s qualified report evidence. Atungbusu remains unmapped because identity is unresolved. Other affected places remain pending |
| 3D, cross-section, swipe and split screen | Advisory small multiples plus real VIIRS single/swipe/side-by-side modes in `SatelliteComparison.tsx`; pointer/keyboard/date-swap/mobile browser test | Swipe, daily snapshot pairing and an interactive east–west altitude cross-section are implemented. Cross-section geometry tests cover disjoint intersections and vertices; browser tests cover latitude, lead and altitude filtering. MapLibre 3D altitude-band extrusion is also implemented and browser-tested with forecast/filter/URL persistence. A shared-clock infrared/Ash RGB pair is implemented with per-pane source times and independent missing-frame clearing, including mobile stacking. The paired replay also compares the same product at fixed offsets (−6, −1, +1, +6 or +24 hours), with advisory geometry selected at each pane’s time and comparison settings preserved in shared URLs. Camera navigation remains independent. Replay share links also preserve altitude band, polygon visibility, full-extent setting and timezone; production browser tests verify reload restoration and invalid-value recovery. |
| General event configuration and Merapi case | Shared catalog types; per-volcano windows; national directory | Replay asset loading, clock bounds and advisory selection now use event configuration. Replay headings, place names, timezone labels, coverage notes and airport summaries now come from the validated event configuration; the map uses its volcano identifier and the processing-manifest link uses its asset path. The loader accepts an event path. Airport location assets now come from event configuration, map bounds derive from the supplied report locations, and missing locations are listed from the actual report identities. A production browser test substitutes an airport asset and verifies that only its supplied location is shown. Airport status is now a separate validated event asset; a missing closure report or an empty airport inventory is supported without inventing operating status. Multiple closure cycles and airport-specific closure intervals still need generalisation; Merapi’s March 2023 advisory dataset is implemented with 10 retained editions; seven daily VIIRS crops and explorer swipe/split comparisons are available. A configurable ten-minute Merapi replay remains pending |
| Event/timeline schema artifacts | Typed consumers, integrity tests and `chronology.json` | `events/anak-krakatau-2026-09/event.json` and `timeline.json` are built offline and consumed by replay. Draft-07 schemas and tests validate structure, dates, asset paths, marker source references and clock boundaries; `data:build` runs this gate. The event also supplies `advisories.geojson`, with source polygons, edition and correction metadata, explicit empty lead assessments, units and temporal-use notes. An integrity test compares every exported edition and component with the retained catalog. `airport-status.json` now preserves the sourced airport reports, nullable closure evidence and limitations, with schema and source-reference checks; the replay consumes it. `observations.geojson` now preserves all six report events with time precision, qualifications and source/publication references. Unspecified locations remain null, including aggregate airport paper-test statements; an integrity test checks every marker against the timeline. Raster and source contracts remain pending. |
| Accessibility and visual/performance verification | Keyboard controls, accessible inputs, screenshots and mobile overflow checks | Automated axe WCAG 2/2.1 A/AA checks cover the five default presentations, expanded historical imagery comparison and selected airport evidence; keyboard dialog focus restoration is checked. Editorial contrast and dialog naming/focus defects were corrected. Noninteractive map labels are no longer inactive buttons; airport buttons have explicit inspection labels. A production Chromium test verifies 200 paired clock changes and records interaction metrics in `docs/replay-performance.md`. Manual assistive-technology review, expanded interaction-state coverage, visual baselines, physical mobile-device/cold-start and prolonged memory measurements remain pending. A throttled 390×844 browser check now exercises 20 paired clock changes with 150 ms latency, 1.5 Mbps bandwidth, disabled cache and a 4× CPU slowdown; both panes settle correctly without JavaScript errors or overflow. Measurements and limits are recorded in `replay-performance.md` |
| Blog embedding | `?embed=1`, base-aware assets and documented iframe example | Strict subpath iframe integration test passes across all five presentations, including worker and image assets, event/timeline contracts, paired replay, historical Semeru imagery, controls and mobile overflow. A working source-linked article draft with catalog-checked event entry points is available in `docs/blog-draft.md`. Final deployment/content integration in the blog repository remains pending. |

## Next substantive work

The full Ash RGB build is complete. Resolve outstanding source reuse terms, preserve the missing morning advisories and airport interval evidence, then extend the shared event configuration to additional source-backed replay archives. Keep the current source-backed views reviewable while making these gaps explicit. Do not claim the original event-reconstruction gate passed, substitute Band 13 for Ash RGB, infer dormant status from missing reports, or treat a scheduled closure as an actual measured interval.
